version: '3.7'

services:
    dev:
        build:
            context: .
        working_dir: /app
        volumes:
            - "..:/app"
        depends_on:
            - db_instance
        command: >
            bash -c "pwsh /app/_dev/init.ps1 && sleep infinity"
    
    db_instance:
        # image: mcr.microsoft.com/mssql/server:2019-latest
        image: ghcr.io/dah-dah-demos/solid-tribble-db@sha256:30253b94232eed22b122651615ee362d452712619b39dcf63a468e81a560b9cc
        environment:
            - ACCEPT_EULA=Y
            - SA_PASSWORD=P@ssw0rd12345
        ports:
            - 7601:1433
